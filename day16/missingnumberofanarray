#include <stdio.h>
int main(){
    int n,sum=0;
    scanf("%d",&n);
    int a[n];
    for(int i=0;i<n;i++){scanf("%d",&a[i]);sum+=a[i];}
    int total=(n+1)*(n+2)/2;
    printf("%d",total-sum);
    return 0;
}
